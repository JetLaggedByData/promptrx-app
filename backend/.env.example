ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# Local dev — matches docker-compose. In production, use your Neon connection string.
DATABASE_URL=postgresql+asyncpg://promptrx:promptrx_dev@localhost:5432/promptrx

# Local dev — matches docker-compose. In production, paste your Upstash rediss:// URL.
REDIS_URL=redis://localhost:6379

JWT_SECRET=change-this-to-32-random-characters-minimum
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
ENVIRONMENT=development
FRONTEND_URL=http://localhost:5173
SENTRY_DSN=

# Email (Gmail SMTP) — contact form delivery (optional)
SMTP_USER=
SMTP_PASSWORD=
CONTACT_EMAIL=
